/**
 * Shared UI primitives — the groundwork for a future uniform rework.
 *
 * Today these are thin Tailwind wrappers; future redesigns just change the
 * primitives in one place instead of every page. Add to this file when you
 * find another pattern repeated across pages.
 */

export { Button } from "./Button"
export { Card } from "./Card"
export { Input, TextArea } from "./Input"
export { Badge, statusTone } from "./Badge"
